---
/**
 * Game Settings Modal Component
 * Allows player to customize game settings including vision mode, character, and theme
 */

import CharacterPreview from "./CharacterPreview.astro";
---

<div id="gameSettingsModal" class="game-settings-modal hidden">
    <div class="game-settings-content">
        <div class="settings-header">
            <span class="settings-gear">⚙️</span>
            <h2>SETTINGS</h2>
        </div>
        
        <!-- Character Options Section -->
        <section class="settings-section">
            <h3><span class="section-icon">🎭</span> CHARACTER:</h3>
            <div class="option-grid six-col">
                <CharacterPreview 
                    dataCharacter="Hami"
                    isSelected=true
                />
                <CharacterPreview 
                    dataCharacter="Guba"
                    isSelected=false
                />
                <CharacterPreview 
                    dataCharacter="Killua"
                    isSelected=false
                />
                <CharacterPreview 
                    dataCharacter="Natsu"
                    isSelected=false
                />
            </div>
        </section>

        <!-- Theme Section -->
        <section class="settings-section">
            <h3><span class="section-icon">🎨</span> THEME:</h3>
            <div id="themeOptionsContainer" class="option-grid three-col">
                <!-- Themes will be dynamically populated from THEMES config -->
            </div>
        </section>

        <!-- Game Test Section -->
        <section class="settings-section" style="border-bottom: none; margin-bottom: 0; padding-bottom: 0;">
            <h3><span class="section-icon">🎮</span> Flute Tests:</h3>
            <div id="testOptionsContainer" class="option-grid four-col">
                <!-- Tests will be dynamically populated based on vision mode -->
            </div>
        </section>

        
        <!-- Action Buttons -->
        <div class="settings-actions">
            <button id="saveGameSettings" class="settings-btn primary">SAVE</button>
            <button id="closeGameSettings" class="settings-btn secondary">CANCEL</button>
        </div>
    </div>
</div>
