---
import siteConfig from '../frontend_config.js'; 
import '../css/home.css';
import '../css/shared.css';
import Navbar from './Navbar.astro';

interface Props {
    selectedPage?: string;
    pageTitle?: string;
}

const { selectedPage = 'home', pageTitle } = Astro.props;
const title = pageTitle ?? siteConfig.site_name; 
---
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title} | FluteVision</title>
    <link rel="icon" type="image/x-icon" href="favicon.svg">
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-8Z7E1RKF4G"></script>
    <script>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());
      gtag('config', 'G-8Z7E1RKF4G');
    </script>
  </head>

<body>
    <div class="floating-emoji">🎶</div>
    <div class="floating-emoji">🎼</div>
    <div class="floating-emoji">🎵</div>   
    <div class="floating-emoji">🎶</div>   
    <div class="floating-emoji">🎼</div>  
    <div class="floating-emoji">🎤</div>   
    <div class="floating-emoji">🎷</div>   
    <div class="floating-emoji">🎶</div>
    <div class="floating-emoji">🎵</div>
    <div class="floating-emoji">🎼</div>
    <div class="floating-emoji">🎷</div>
    <div class="floating-emoji">🎻</div>   
    <div class="floating-emoji">🎶</div>
    <div class="floating-emoji">🎵</div>
    <div class="floating-emoji">🎼</div>
    <div class="floating-emoji">🌟</div>  
    
    <Navbar selectedPage={selectedPage}/>

    <main>
        <slot />
    </main>
    <script src="../js/api/api.js"></script>
    <script src="../js/navigation.js"></script>
    <script src="../js/featureCardColors.js"></script>
</body>
</html>

